interface:
  display_name: "Multi-Agent Sync"
  short_description: "Synchronize shared configuration across coding agents."
  default_prompt: "Use $multi-agent-sync to inspect profile ownership, preview and apply shared Agent configuration changes, bootstrap host-local hooks, and validate portability before handoff."
