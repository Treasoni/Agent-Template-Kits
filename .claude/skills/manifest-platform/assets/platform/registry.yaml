apiVersion: agent-platform/v1
discovery:
  kindDirectories:
    Workflow: .codex/workflows
    Skill: .claude/skills
    Subagent: .codex/agents
    Hook: .claude/hooks
policy:
  allowedPermissions:
    filesystem:
      - none
      - read
      - write
    network:
      - none
      - allow
    subprocess:
      - none
      - allow
    git:
      - none
      - read
      - write
  hookEvents:
    - SessionStart
    - Stop
